== Getting Started

1. Run 'bundle install' in your application directory.

2. Run 'rake db:create' and 'rake db:migrate' to create required migrations.

3. Run 'rake setup:data' to import default data into database.

4. Run 'rails s' to start the application server.
 
5. You can browse the application through localhost:3000

